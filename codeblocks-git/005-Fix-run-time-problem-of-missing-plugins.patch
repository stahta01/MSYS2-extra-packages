From 715814dbb19379b7bdc25cd74490a425b0ef77fb Mon Sep 17 00:00:00 2001
From: Tim Stahlhut <stahta01@gmail.com>
Date: Fri, 10 Apr 2026 18:07:27 -0400
Subject: Fix run-time problem of missing plugins

---
 src/sdk/pluginmanager.cpp | 10 ++++++++++
 1 file changed, 10 insertions(+)

diff --git a/src/sdk/pluginmanager.cpp b/src/sdk/pluginmanager.cpp
index 4cd897618..875885adc 100644
--- a/src/sdk/pluginmanager.cpp
+++ b/src/sdk/pluginmanager.cpp
@@ -784,6 +784,11 @@ bool PluginManager::ReadManifestFile(const wxString& pluginFilename,
 #endif
             actual.Remove(0, 3);
 
+#if defined(__MSYS__)
+        if (actual.StartsWith("msys-"))
+            actual.Remove(0, 5);
+#endif
+
         actual = ConfigManager::LocateDataFile(actual, sdPluginsUser | sdDataUser | sdPluginsGlobal | sdDataGlobal);
         if (actual.empty())
         {
@@ -952,6 +957,11 @@ void PluginManager::ReadExtraFilesFromManifestFile(const wxString& pluginFilenam
 #endif
         actual.Remove(0, 3);
 
+#if defined(__MSYS__)
+    if (actual.StartsWith("msys-"))
+        actual.Remove(0, 5);
+#endif
+
     actual = ConfigManager::LocateDataFile(actual, sdPluginsUser | sdDataUser | sdPluginsGlobal | sdDataGlobal);
     if (actual.empty())
     {
-- 
2.53.0.windows.2

